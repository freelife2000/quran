# ⚡ دليل البدء السريع - Quick Start Guide

## 🚀 البدء الفوري في 3 خطوات

### 1️⃣ تثبيت المتطلبات
```bash
cd /workspaces/quran
pip install -r requirements.txt
```

### 2️⃣ تشغيل الخادم
```bash
cd /workspaces/quran
python app/server.py
```

ستظهر هذه الرسالة:
```
INFO:     Uvicorn running on http://0.0.0.0:8000
```

### 3️⃣ فتح التطبيق
افتح المتصفح واذهب إلى:
```
http://localhost:8000
```

---

## 🎯 الميزات الأساسية

### 📖 قراءة الآيات
1. افتح التطبيق
2. اختر سورة من القائمة
3. أدخل رقم الآية
4. اختر ترجمة (اختياري)
5. اضغط "عرض الآية"

### 🔍 البحث المتقدم
1. انقر على تبويب "البحث"
2. اكتب الكلمة المراد البحث عنها
3. اختر ترجمة إن أردت
4. اضغط "بحث"

### 📱 تصفح السور
1. انقر على تبويب "تصفح السور"
2. اختر السورة بالنقر عليها
3. سترى جميع الآيات بالترتيب

### 🌙 تبديل الوضع الليلي
- تم حفظ التفضيل تلقائياً
- يتم استعادته عند الزيارة القادمة

---

## 🛠️ المتطلبات

```
FastAPI >= 0.95.0
Uvicorn >= 0.21.0
```

جميع المتطلبات محددة في `requirements.txt`

---

## 📁 البيانات

التطبيق يستخدم البيانات التالية:
- ✅ `data/quran.json` - النص الكامل
- ✅ `data/verses_with_tafsir.json` - التفاسير
- ✅ `data/translations/` - الترجمات (80+ ترجمة)
- ✅ `data/prayer_times.json` - أوقات الصلاة
- ✅ `data/ahkam_adhkar.json` - الأحكام والأذكار

---

## 🔧 الإعدادات

### تغيير المنفذ
```bash
# تشغيل على منفذ مختلف
uvicorn app.server:app --port 3000 --reload
```

### تشغيل في بيئة الإنتاج
```bash
# استخدام Gunicorn (أسرع)
gunicorn app.server:app --workers 4 --bind 0.0.0.0:8000
```

---

## 🐛 استكشاف الأخطاء السريع

| المشكلة | الحل |
|--------|------|
| Connection refused | تأكد من تشغيل الخادم |
| 404 من الترجمات | فعّل ترجمة أخرى من القائمة |
| الصفحة لا تحمل | امسح cache المتصفح (Ctrl+Shift+Delete) |

---

## 📞 الدعم

للمساعدة، تحقق من:
1. `GUIDE.md` - الدليل الشامل
2. `README.md` - الدليل الأساسي
3. `app/server.py` - كود الخادم مع التعليقات

---

**استمتع! 🎉**
