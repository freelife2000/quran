# 📖 دليل تطبيق القرآن الكريم - Quran App Guide

تطبيق ويب حديث وشامل للقرآن الكريم مع ترجمات وتفاسير متعددة اللغات وواجهة مستخدم متقدمة تتناسب مع جميع الدول والأجهزة.

## ✨ الميزات الرئيسية

### 🎨 واجهة مستخدم عصرية
- تصميم **Responsive** يعمل على جميع الأجهزة (الهاتف، التابلت، الحاسوب)
- دعم الوضع الليلي (**Dark Mode**) والوضع الفاتح
- تخزين تفضيلات المستخدم محلياً

### 📱 دعم اللغات والاتجاهات
- دعم كامل **RTL** (من اليمين إلى اليسار) للعربية
- دعم **LTR** للغات الإنجليزية والأجنبية
- ترجمات متعددة اللغات (الإنجليزية، الفرنسية، الألمانية، وغيرها)

### 🔍 ميزات البحث المتقدمة
- بحث **سريع** في القرآن الكريم
- بحث في ترجمات محددة
- عرض النتائج بشكل منظم وسهل القراءة

### 📚 محتوى شامل
- **114 سورة** كاملة من القرآن الكريم
- **تفاسير** للآيات
- **ترجمات متعددة** (أكثر من 80 ترجمة)
- **أوقات الصلاة** والمعلومات الإسلامية
- **أحكام وأذكار** إسلامية

### 🚀 الأداء
- تحميل سريع للصفحات
- استخدام **API محسّن** للاستعلامات
- **Cache** محلي للبيانات المستخدمة بكثرة

## 🛠️ التثبيت والتشغيل

### المتطلبات
- Python 3.8+
- FastAPI
- Uvicorn

### خطوات التثبيت

1. **استنساخ/فتح المشروع:**
```bash
cd /workspaces/quran
```

2. **تثبيت المتطلبات:**
```bash
pip install -r requirements.txt
```

3. **تشغيل الخادم:**
```bash
python app/server.py
```

أو باستخدام uvicorn:
```bash
uvicorn app.server:app --reload --host 0.0.0.0 --port 8000
```

4. **فتح التطبيق:**
- افتح المتصفح واذهب إلى: `http://localhost:8000`

## 📖 استخدام التطبيق

### 1️⃣ عرض الآيات
- اختر السورة من القائمة المنسدلة
- أدخل رقم الآية
- اختر ترجمة (اختياري)
- اضغط على "عرض الآية"

### 2️⃣ تصفح السور
- انقر على تبويب "تصفح السور"
- اختر السورة التي تريد
- ستظهر جميع الآيات بالترتيب

### 3️⃣ البحث
- انقر على تبويب "البحث"
- أدخل كلمة أو عبارة للبحث
- اختر ترجمة للبحث بها (اختياري)
- اضغط على "بحث"

### 4️⃣ أوقات الصلاة
- اضغط على زر "أوقات الصلاة"
- ستظهر نافذة بأوقات الصلاة

### 5️⃣ الأحكام والأذكار
- اضغط على زر "أحكام وأذكار"
- ستظهر نافذة بالأحكام والأذكار الإسلامية

## 🎨 تخصيص الوضع الليلي

يمكنك تبديل الوضع الليلي بواسطة:
- تشغيل الدالة `toggleTheme()` في وحدة تحكم المتصفح
- هذا سيحفظ التفضيل محلياً

## 🔧 البنية المعمارية

```
quran/
├── app/
│   └── server.py          # خادم FastAPI الرئيسي
├── data/
│   ├── quran.json         # نص القرآن الكريم كاملاً
│   ├── verses_with_tafsir.json  # الآيات مع التفاسير
│   ├── prayer_times.json  # أوقات الصلاة
│   ├── ahkam_adhkar.json  # الأحكام والأذكار
│   └── translations/      # مجلد الترجمات
├── web/
│   ├── index.html         # الصفحة الرئيسية
│   └── app.js             # كود JavaScript الرئيسي
├── requirements.txt       # المتطلبات
└── README.md             # الدليل الأساسي
```

## 📡 نقاط نهاية API

### الآيات والترجمات
- `GET /api/surahs` - قائمة السور
- `GET /api/verse/{sura}/{ayah}` - آية محددة
- `GET /api/translation/{code}/{sura}/{ayah}` - ترجمة آية
- `GET /api/tafsir/{sura}/{ayah}` - تفسير آية
- `GET /api/translations` - قائمة الترجمات

### البحث
- `GET /api/search?q={query}` - البحث في القرآن
- `GET /api/search?q={query}&translation={code}` - البحث في ترجمة

### معلومات إضافية
- `GET /api/prayer-times` - أوقات الصلاة
- `GET /api/ahkam-adhkar` - الأحكام والأذكار

## 🌐 التوزيع على الإنترنت

### استخدام Docker
```bash
docker-compose up
```

### أو استخدام خادم منتج
```bash
gunicorn app.server:app --workers 4 --bind 0.0.0.0:8000
```

## 📱 التوافق

| المتصفح | الدعم |
|---------|-------|
| Chrome | ✅ كامل |
| Firefox | ✅ كامل |
| Safari | ✅ كامل |
| Edge | ✅ كامل |
| أندرويد Chrome | ✅ كامل |
| iOS Safari | ✅ كامل |

## 🐛 استكشاف الأخطاء

### المشكلة: الصفحة لا تحمل
**الحل:** تأكد من أن الخادم يعمل على `http://localhost:8000`

### المشكلة: الترجمات لا تظهر
**الحل:** تحقق من أن ملفات الترجمات موجودة في `data/translations/`

### المشكلة: البحث لا يعطي نتائج
**الحل:** جرّب ترجمة مختلفة أو تحقق من البيانات

## 🤝 المساهمة

لتحسين التطبيق:
1. ابدأ بإضافة ميزات جديدة
2. اختبر جيداً
3. وثّق التغييرات

## 📝 الترخيص

جميع الحقوق محفوظة © 2024

---

**استمتع باستخدام التطبيق! 📖✍️**
